import 'package:flutter/material.dart';

class AppColors {
  // 🌿 BRAND
  static const primary = Color(0xFF4E7C6F); // Sage Green
  static const secondary = Color(0xFF9DB8AE); // Soft Mint
  static const accent = Color(0xFFE6F0EC);

  // 🧱 SURFACE
  static const background = Color(0xFFF6F7F6);
  static const surface = Colors.white;
  static const card = Colors.white;

  // ✍️ TEXT
  static const textPrimary = Color(0xFF1F2D2A);
  static const textSecondary = Color(0xFF6B7C77);
  static const textMuted = Color(0xFF9AA8A3);

  // ⚠️ STATE
  static const success = Color(0xFF3FA796);
  static const warning = Color(0xFFE8B86D);
  static const error = Color(0xFFD9534F);
  static const Color danger = Color(0xFFF44336);
}
